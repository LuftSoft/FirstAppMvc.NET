@{
    string message = TempData["_statusMessage"].ToString();
    if (!string.IsNullOrEmpty(message))
    {
        <div class="alert alert-warning" role="alert">
            @message
        </div>
    }
}
